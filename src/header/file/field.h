#ifndef FIELD_H
#define FIELD_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_FIELD_LENGTH 100
#define MAX_USER 100
#define MAX_LINE_LENGTH 256

typedef char fields[MAX_FIELD_LENGTH];

#endif